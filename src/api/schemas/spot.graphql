type Spot {
  id: ID
  gap: Gap
  shelf: Boolean
}

type Mutation {
  createSpot(id: ID!, gap: ID!, shelf: Boolean!): Spot
  updateSpot(id: ID!, gap: ID, shelf: Boolean): Spot
  deleteSpot(id: ID!): Spot
}
